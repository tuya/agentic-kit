# Test Resources

Resource files used by posix examples. Run demos from the `examples/posix/` directory so the default `res/` paths resolve correctly.

| File | Used by |
|------|---------|
| `test.jpg` | `edu_camera_demo`, `tai_edu_camera_demo` |
| `qr.jpg` | `scan_by_device_pair_demo` |
